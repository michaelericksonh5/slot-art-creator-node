{
  "name": "node-domexception",
  "version": "1.0.99",
  "description": "Local shim — re-exports the native globalThis.DOMException available on Node >=18. Wired in via npm 'overrides' in the parent package.json so the deprecated upstream package never installs.",
  "main": "index.js",
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
